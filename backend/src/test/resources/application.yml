server:
  error:
    include-message: always

spring:
  datasource:
    url: jdbc:postgresql://localhost:5/quickcheck-unit-test
    username: quickcheck
    password: password
    hikari:
      maximum-pool-size: 5